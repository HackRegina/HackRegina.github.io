import { SponsorshipLevelPipe } from './sponsorship-level.pipe';

describe('SponsorshipLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new SponsorshipLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
